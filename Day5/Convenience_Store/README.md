물건클래스(조상)-> 구체적인 물건(후손)->객체->리스트저장->관리(등록/삭제/검색)
조상클래스 코딩방법(abstract)
후손클래스 (오버로딩/오버라이딩)
==================================================
프로젝트 이름 (Convenience_Store)
==================================================
패키지(Convenience_Store_Package)
================================
클래스 이름(Product)

int serial_no //고유번호
char name[10] //물건 이름
int price //가격
int quantity //개수
char expired[10]//유통기한
======================================================
프로그램 작동부분 클래스(Play)
*Product 클래스로 물건정보를 여러개 저장해서, 그것들을 전부 리스트 ArrayList에 저장해둔다.
*저장되 객체들을 전부 특정파일에 저장하도록 한다.
*파일로부터 저장된 객체정보들을 읽어와서 화면에 전부 출력해주도록 한다.